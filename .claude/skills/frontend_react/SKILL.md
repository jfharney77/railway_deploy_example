# Frontend React Skill

Use React with Vite and TypeScript.

Preferred structure:

frontend/
  src/
    api/
    components/
    pages/
    hooks/

Requirements:
- Use VITE_API_BASE_URL for backend URL
- Keep API logic in src/api
- Add package.json scripts for dev, build, and start
- Use serve -s dist -l $PORT for Railway