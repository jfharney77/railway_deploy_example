# Repo Structure Skill

Use this skill when creating or reorganizing the repository.

Expected structure:

- /backend for FastAPI
- /frontend for React + Vite
- separate Dockerfiles per service
- separate .env.example files per service
- root README.md with local run + Railway deploy instructions

Backend Railway root directory: /backend
Frontend Railway root directory: /frontend